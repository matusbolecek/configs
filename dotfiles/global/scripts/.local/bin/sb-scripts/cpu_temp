#!/bin/sh
temp=$(sensors 2>/dev/null | awk '
  /^Package id 0/ { gsub(/[+°C]/, "", $4); print int($4); exit }
  /^Tctl/         { gsub(/[+°C]/, "", $2); print int($2); exit }
')
[ -z "$temp" ] && exit 0
printf "%s°C \n " "$temp"
